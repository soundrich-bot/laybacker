- Audio-only: the NORM settings now open on the first click, not the second.
- New **Clock** mode for audio files — it checks your levels and head/tail silence, and once they pass it adds a 10-second silent handle to the front and 5 seconds to the end for delivery.
