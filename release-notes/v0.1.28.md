- Clock now works across a whole batch — hit CLOCK ALL and Laybacker checks each file and adds the 10-second/5-second handles to the ones that pass.
- Output filenames no longer say "normalised" — they just carry the spec, e.g. `MyMix_-23LUFS_-1dBTP.wav`.
