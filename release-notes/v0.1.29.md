- NORM ALL now normalises everything to the QC loudness value you've set, and the filename says so (e.g. `MyMix_-23LUFS_-1dBTP.wav`).
- Clocked files keep their level exactly as it is — clocking only adds the handles — and the output is named `MyMix_Clocked.wav`.
