- Clock: the −1 dBTP limit is now treated as a ceiling (peaks just stay under it), with your loudness target (e.g. −23 LUFS) as the level it checks.
- Clock: if a file isn't quite on spec, it now tells you the measured level and lets you proceed anyway.
